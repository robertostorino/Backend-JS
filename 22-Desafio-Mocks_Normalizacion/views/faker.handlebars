<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/js/bootstrap.bundle.min.js" integrity="sha384-OERcA2EqjJCMA+/3y+gxIOqMEjwtxJY7qPCqsdltbNJuaOe923+mo//f6V8Qbsw3" crossorigin="anonymous"></script>
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="icon" type="image/x-icon" href="./images/node-js.ico">


    <script src="/socket.io/socket.io.js"></script>
    <title>Faker Product List by Faker.js</title>
</head>

<body>
    <section class="container-fluid d-flex flex-row justify-content-center" style="margin-top: 2rem;">
        {{#if listExist}}
        <h3 style="color: blue; text-aling: center">Lista de Productos Fake</h3>
        <div class="table-responsive container" style="width: 40%;">
            <table class="table table-success">
                <thead>
                    <tr>
                        <th scope="col">Producto</th>
                        <th scope="col">Precio</th>
                        <th scope="col">Imagen</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each products}}
                        <tr>
                            <td>{{this.title}}</td>
                            <td>{{this.price}}</td>
                            <td><img style="width: 50px" src="{{this.thumbnail}}" alt="{{this.thumbnail}}"></td>
                        </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        {{else}}
            <hd class="aler alert-danger container">No hay productos</hd>
        {{/if}}
    </section>
</body>

<style>
    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
    -webkit-appearance: none;
    margin: 0;
    }

    input[type=number] {
    -moz-appearance: textfield;
    }

    th, td {
        text-align: center;
    }

</style>

</html>